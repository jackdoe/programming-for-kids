while True:
    sum = 0
    name1 = input('name 1: ')
    name2 = input('name 2: ')

    for c in name1:
        sum += ord(c)
    for c in name2:
        sum += ord(c)        
    
    match = 1 + (sum % 100)
    print("love test match:")
    print(match)
